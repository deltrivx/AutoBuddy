# 更新日志

本项目的所有重要变更都记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

**这份日志只写两件事：新增了什么、修复了什么。** 部署与升级请看 [README](./README.md)。

---

## [未发布]

### 计划中

- 暂无。

## [v0.5.8] - 2026-09-24

### 账号接入页面全面优化与 GitHub 注册全自动化
- **浏览器环境标识修正**：界面文案由 Playwright 更正为 Patchright 免检测驱动，与实际底层一致。
- **代理配置去重**：删除冗余的「Clash REST API 地址」输入框与「禁用自动切换 Clash 节点」复选框，后端同步移除节点自动切换逻辑与相关配置项，配置只保留「注册专用出网代理」一项。
- **新增代理检测**：在代理配置旁增加「检测代理」按钮，通过该代理实测 GitHub 与 GitHub API 可达性，实时返回可用状态与延迟。
- **设备验证码全自动化**：新增邮件正文 8 位 Launch Code 提取逻辑，注册流程改为「先建临时邮箱 → 再注册 → 自动收信取码回填」，前端「GitHub 设备验证码」输入框已移除，无需人工介入。
- **补齐风控对抗配置**：新增「打码最大重试轮数」与「表单填充节流基数」两项配置，前者支持验证码多轮换题重试，后者降低表单瞬间提交带来的风控评分。

## [v0.5.7] - 2026-09-24

### 浏览器底层升级为 Patchright 免检测驱动
- **底层驱动迁移**：将 Playwright 升级为 Patchright（Undetected Python Automation），专用于绕过自动化风控探测与反爬检测。
- **免内置自动下载架构**：镜像仅安装 Patchright 核心驱动，浏览器内核保持动态下载至持久化挂载卷，容器启动时自动拉取 Patchright 专用 Chromium 内核。
- **前端 UI 零破坏保持**：前端保持原样下载交互与进度条展示，底层执行 `python3 -m patchright install chromium`。

## [v0.5.6] - 2026-09-24

### 登录安全增强与配置修正
- **默认凭据调整**：控制台默认密码由 `admin123` 调整为 `password`，模板与后端保持一致。
- **登录框去预填与去提示**：账号、密码输入框不再预填任何默认值，也不再显示默认账号/密码提示文案，避免敏感信息泄露。
- **新增滑动验证**：参考 Octop 登录页交互，增加内联 SVG 拖拽滑块验证（纯前端实现，无外部依赖），未完成滑动校验无法提交登录。
- **Clash REST API 地址修正**：前端表单默认值与回填逻辑统一校正为 `http://192.168.31.10:9090`，不再指向不存在的 `127.0.0.1:9090` / `192.168.31.1:9090`。

## [v0.5.5] - 2026-09-24

### 代理架构优化与模板脱敏修复
- **取消全局代理环境导出**：容器环境及 entrypoint 彻底移除全局 HTTP_PROXY / HTTPS_PROXY 导出，模型网关（18091）与官方后端直连上游，根除代理连接拒绝引起的 ConnectTimeout 异常。
- **代理配置收敛至注册页面**：在 WebUI「账号接入」面板增加「注册专用出网代理 (register_proxy)」配置项，Playwright 自动化注册时按需挂载代理，默认指向 OpenClash 节点 `http://[IP]:7890`，Clash REST API 校准为 `http://[IP]:9090`。
- **Unraid 模板全面脱敏与代理清理**：Unraid 容器模板取消 HTTP_PROXY / HTTPS_PROXY / NO_PROXY 变量；管理员账号密码严格使用占位默认值 `admin` / `admin123`，彻底消除脱敏替换标记和覆盖用户本地配置的问题。

## [v0.5.4] - 2026-09-24

### 自动打码与账号闭环接入
- **自动打码平台集成**：集成主流打码服务（CapSolver / 2Captcha / 自定义端点），在 GitHub 注册触发 Arkose FunCaptcha 旋转拼图验证码时自动异步求解并注入 Token 完成提交。
- **配置持久化与 WebUI 交互**：在 Web 控制台「账号接入」面板新增打码平台类型、Client Key 与自定义 API 端点配置，数据入库 SQLite 持久化存储。
- **WorkBuddy OAuth 自动化入库**：GitHub 注册完成后自动拉起 WorkBuddy AI 国际版授权流，自动点击 Authorize 确认并将生成的凭据自动存入 `accounts.json` 账号池。

## [v0.5.3] - 2026-09-23

### 统计校准与修复
- 修复 Token 统计国内版与国际版 Tab 数据混淆与 100% 重复的问题：`workbuddy` 与 `workbuddy-ai` 严格按账号 `variant`（国内版 `cn` / 国际版 `ai`）分别独立归集计算指标与趋势。
- 修复积分统计（`/api/credits/stats`）混入已删除历史僵尸账号问题：响应层自动根据当前真实账号池过滤，校准剩余额度与用量汇总。

### 模板与安全认证优化
- 强制控制台访问认证，取消无意义的认证开关变量。
- 优化 Unraid 容器模板各项配置的中文描述与说明。

## [v0.5.2] - 2026-09-23

### 认证增强
- 支持通过环境变量 `AUTH_USERNAME`（或 `AUTH_USER`）与 `AUTH_PASSWORD`（或 `AUTH_PASS`）配置登录凭据。
- 未配置环境变量时，默认用户名和密码均为 **`[密钥]`**。
- Unraid 模板同步增加 `AUTH_USERNAME` 与 `AUTH_PASSWORD` 配置项。

## [v0.5.1] - 2026-09-23

### 认证与数据库
- 内置 SQLite 数据库（`/data/.autobuddy/autobuddy.db`），持久化存储用户认证与系统配置。
- WebUI 增加管理员登录认证（默认用户 `admin`，初始密码 `admin123`），支持密码修改与会话管理。
- 环境变量精简：自动化注册相关配置（邮箱 API、Cloudflare 凭据、代理节点等）直接在 WebUI 保存至 SQLite 数据库，避免在 Docker / Unraid 模板中繁琐堆砌。

### 代理环境与精确分流
- 容器启动脚本（`entrypoint.sh`）注入完整代理支持（`HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY`）。
- 严格遵循内网防回环与 Python SDK/httpx 分流约束，`NO_PROXY` 默认预置精确 IP 列表（`localhost,127.0.0.1,::1,192.168.31.2,192.168.31.1...`），禁止 CIDR 网段，确保内网互通与出网代理互不干扰。

### 品牌细节
- 修复侧边栏左上角旧版残留文字：通过响应层字节过滤与前端 DOM 实时对齐，彻底更新为 **AutoBuddy**。

## [v0.5.0] - 2026-09-23

### 更名
- 项目更名为 **AutoBuddy**（原 WorkBuddy Switch）。更名覆盖：仓库、镜像、容器名、
  Unraid 模板、WebUI 标题与「关于」面板、README 与文档。
- 数据目录由 `/data/.wb-switch` 迁移为 `/data/.autobuddy`；环境变量前缀由 `WB_` 改为 `AB_`
  （`AB_DATA_DIR` / `AB_VERSION` / `AB_PROJECT_URL` / `AB_PROJECT_NAME`）。
- Unraid 模板文件按 Unraid 要求使用全小写：`unraid/autobuddy.xml`。
- 底层仍是官方 `workbuddy-switch` 包，仅在其之上做容器化定制，未改动官方能力边界。

### 浏览器内核
- 下载进度结构化（百分比 / 已下载字节 / 总字节 / 阶段 / 已用时），
  进度条行不再写入日志，日志只保留关键输出。
- 修复双进程同时下载导致 `__dirlock` 争抢、目录长期为空且进度卡死的问题：
  内核下载统一由注册服务 startup 钩子托管，`entrypoint.sh` 不再自行调用 playwright。
- 「账号接入」页面：未安装时显示实时进度条并自动轮询，已安装时显示持久化路径与内核目录。

---

---

## [v0.4.17] - 2026-09-23

<!-- summary: 新增 GitHub 账号自动化接入，浏览器改为运行时下载到挂载卷 -->

### 新增

- **GitHub 账号自动化接入（容器内一站式）**：WebUI 新增「账号接入」面板，
  在容器内直接跑完 Google OAuth 登录 → 注册 GitHub → 绑定域名邮箱 →
  收验证邮件 → 设主邮箱 → 删旧邮箱的完整流程。浏览器由容器内置驱动直接
  launch（headless），**不外接任何 CDP**，不依赖宿主机桌面环境或 9222 端口。
  邮箱验证走 Cloudflare D1 收件箱；节点轮换走 OpenClash REST，不再依赖
  Windows 命名管道，可在纯 Linux 容器内运行。

- **浏览器改为运行时下载**：镜像不再打包 Chromium，只装 Playwright 驱动与
  headless 运行时系统库；WebUI「账号接入」面板提供「下载浏览器」按钮，
  下载到挂载卷并持久化。镜像因此明显瘦身，构建更快、推拉更轻，
  且容器重建或升级都不必重下浏览器。

### 变更

- **新增浏览器缓存挂载路径**：容器新增 `/data/.autobuddy/browsers` 挂载点，
  Unraid 模板对应新增「浏览器缓存目录」配置项
  （默认 `/mnt/user/appdata/autobuddy/browsers`）。
- 注册服务跑在容器内网 `18092`，由 WebUI 反代转发，**不对外映射端口**；
  对外仍只有 `18090` / `18091`。

---


## [v0.4.16] - 2026-09-21

<!-- summary: 修正账号被拦截的判定与措辞，重扫无效的说明移到设置页 -->

### 修复

- **「账号被拦截」的措辞不再指向错误方向**：此前把上游 403 / 错误码 11140
  称为「账号被风控」。实测表明这个说法会把人引向两个都不成立的方向 ——
  平台风控通常针对内容或频率，而发一句 `hi`、甚至发一个不存在的模型名同样被拦，
  与请求内容、频率、请求头均无关。真正的判定发生在模型校验之前，是**账号级拦截**。
  界面文案统一改为「账号被上游拦截」，不再使用「风控」一词。

- **明确「重新扫码无效」**：同一账号换一张全新凭据后仍被拦截（凭据指纹已变、
  拦截依旧），说明拦的是账号本身而非凭据。此前界面提示「重新登录没用」，
  容易被读成「换个方式登录也许可以」。现在在巡检设置的说明区写明：
  重新扫码登录**不会**解除拦截，需要的处置是换一个账号。

- **账号被拦截的提示不再拖长句**：提示条只保留一句结论
  （「账号被上游拦截，暂时用不了」），处置建议移到设置页的说明区 ——
  提示条一闪而过，读不了长文；设置页才有版面把前因后果讲清楚。

### 说明

- **拦截状态照旧自动停用**：探测到账号被拦截时自动停用该账号（可关），
  账号恢复正常后自动放回轮询。判定标准与处理动作本轮未变，只改措辞与呈现位置。

### 新增

- **设置页新增「账号被拦截时怎么办」说明区**：讲清三件事 ——
  拦截发生在模型校验之前、与请求内容和频率无关、重新扫码无效。
  与既有的「登录过期」说明区并列，避免两种状态被混为一谈。

---

## [v0.4.15] - 2026-09-21

<!-- summary: 修复设为首选清空账号白名单；新增模型一键恢复 -->

### 新增

- **模型一键恢复**：账号卡片的「可用模型」标题行新增「全部恢复 N」按钮，
  一次把该账号被禁用的模型全部放回轮询 —— 不区分手动禁用与巡检禁用。
  原先只能逐个点模型标签，巡检误禁一批时点起来很碎。按钮仅在存在禁用项时出现。
  另提供 `scope=auto` 的接口用法，只放回巡检写的那些、保留人工决策；
  恢复前会自动留底，便于回溯改了哪些。

### 修复

- **「设为首选」不再清空账号白名单**：界面上点「设为首选」只提交
  `mode` 与 `manualAccountId`，而服务端此前用「缺字段即取默认值」的方式读入参，
  缺字段被读成空数组并**覆盖落盘** —— 用户勾选的账号白名单在一次「设为首选」后
  整份丢失。空数组在业务上表示「全部启用」，所以现场看不出任何异常，
  只在后续排查时表现为「首选账号设置没生效」。现在服务端以已保存的配置为底，
  只覆盖调用方明确提交的字段；前端也补上 `enabledAccountIds`，两层都不再依赖
  「缺省即重置」这种危险语义。

- **切到手动分配但未指名账号时退回自动分配**：过去这种情况会直接报错，
  于是「只想改白名单」的请求会因为历史配置里没有首选账号而失败。

- **恢复接口的 `disabledModels` 统一为数组**：此前该字段在恢复接口里返回集合、
  在禁用切换接口里返回列表，同一字段两种形状，调用方要写两套判空。

---

## [v0.4.14] - 2026-09-20

<!-- summary: 异常账号的提示收成一句，详情从提示里移出 -->

### 修复

- **异常账号的提示不再冗长**：出错时的提示此前会把三句意思相同的话依次拼上 ——
  结论文案、判定依据、处理建议，读起来要读三遍才知道发生了什么。现在提示只保留
  一句结论，判定依据与完整成因移到悬浮说明与接口里，需要排查时仍可查。

- **建议不再复述结论**：风控账号的建议由「重新登录没用，需要确认账号状态」保留，
  凭据失效档去掉了与结论重复的「重新登录即可恢复」—— 结论已经说了要重新登录，
  再说一遍只是让提示变长。

- **术语统一为「账号被风控」**：自检面板与接口说明里的「账号受限」统一改成
  「账号被风控」，与提示口径一致。

---

## [v0.4.13] - 2026-09-20

<!-- summary: 提示改说人话，风控账号纳入自动停用，恢复正常才自动放回 -->

### 修复

- **检测提示改说人话**：正常账号的提示此前是「凭据有效（用一个不存在的模型名试；
  上游如实回应『该模型不存在』…）」—— 探测手段属于实现细节，摆在结论旁边反而
  把「账号正常」读成了故障。现在只讲用户关心的两件事：**能不能用**、**为什么**。
  正常账号一句话说完；出错时再补原因与建议。

- **风控直接说风控**：账号被上游风控或内容安全拦截时，提示由「凭据有效，但请求
  被上游拦截」改为「账号被上游风控拦截，暂时用不了」。前者要读完并理解「凭据」
  与「请求」的区别才能判断该怎么办，后者一眼就知道问题出在账号本身。

- **风控账号纳入自动停用**：巡检此前只停「凭据失效」的账号，被风控的账号（凭据
  有效但请求一律被拦）会继续参与轮询，持续失败并污染同账号下模型的探测结果。
  现在风控与凭据失效同等处理，一并自动停用并列入受限清单。

- **只有账号恢复正常才自动放回轮询**：新增账号级自动启用，判据是**账号级探测**
  结论为正常 —— 不是「名下某个模型偶然通过」。风控是账号级状态，个别模型通过
  不代表能用。人工停用的账号仍受保护，巡检不会替人放开。

- **账号被风控时模型不再被自动启用**：模型级自愈此前只看模型维度，账号整体被风控
  时仍会把偶然探测通过的模型放回轮询，下一轮又失败。现在账号受限期间冻结自动启用。

### 变更

- 巡检结果新增 `accountsEnabled` 字段，列出本轮被自动放回轮询的账号。
  此前只能看到账号被停用，看不到它何时恢复。

---

## [v0.4.12] - 2026-09-20

<!-- summary: 修正检测提示的配色与措辞：错误档改对类名、三档配色生效、窄屏不再溢出 -->

### 修复

- **错误档配色从未生效**：提示条的调用方生成 `wb-api-toast-error` 类名，
  而样式表里定义的是 `.wb-api-toast-err` —— 两个名字对不上，红色档一次也没命中过。
  凭据失效的提示因此落到默认档，看起来与普通提示无异。已统一为 `-err`，
  并让样式表同时兼容 `-error` 写法，避免调用方再写错时静默降级。

- **提示配色与结论一一对应**：提示条的配色此前只有「默认」与「红色错误」两档，
  账号被上游内容审查拦截时也落入红色错误档，与「凭据已失效」看起来毫无区别。
  现在按结论分三档：正常为绿色、受限为琥珀色、失效为红色，颜色即结论。

- **提示条在移动端不再溢出**：提示条此前未限制宽度、不换行，长结论在窄屏上
  被拉成一条超出屏幕的横条，背景随之被裁切。现在限制宽度并允许换行，
  圆角也改为常规提示条的样式。

- **「凭据有效」不再附带读作故障的说明**：正常账号此前提示「凭据有效，但上游以
  未知模型拒绝」—— 后者本意是说明判定依据（探测刻意用一个不存在的模型名），
  措辞却让人以为上游在报错。现在改为直接说明探测方式，不再出现「拒绝」「失败」一类字眼。

---

## [v0.4.11] - 2026-09-20

<!-- summary: 统一账号有效性判定标准，修正内容审查被误判为凭据失效 -->

### 新增

- **一致性自检**：巡检面板新增入口，一次列出所有账号的「凭据」与「模型」双向结论，
  并附判定依据与建议。凭据有效但模型不可用、账号被上游拦截等情况不再需要自行推断。

  | 结论 | 含义 | 建议 |
  |:---|:---|:---|
  | 凭据有效 | 上游以「模型不存在」应答，说明已认下凭据 | 无需处理 |
  | 账号受限 | 凭据有效，但请求被上游拦截 | 重新登录无效，需确认账号状态 |
  | 凭据已失效 | 上游以鉴权类错误拒绝 | 重新登录 |

- **探测结论按语义分类**：新增错误码语义表，账号与模型两侧的判定都从这一处派生，
  同一份响应在界面各处得到同一结论。

### 修复

- **内容审查不再被误判为凭据失效**：账号被上游内容安全策略拦截时返回 403，
  此前一律按「凭据失效」处理并提示重新登录 —— 而重新登录对此毫无帮助。
  现在单独识别为「账号受限」，与凭据失效区分开。

- **巡检不再因内容审查而批量禁用模型**：同一原因曾让被拦截账号名下的模型
  被成批标记为不可用并写入禁用策略，需要逐项手动恢复。

- **账号凭据探测改用与真实调用一致的请求形态**：此前探测使用非流式请求，
  上游对非流式明确拒绝，探测因此从未真正到达判定点，却仍给出「凭据有效」的结论。

- **账号级探测结果覆盖模型级推断**：凭据有效性只由专门的账号探测裁定，
  不再从模型探测结果反推。此前同一个账号在一轮巡检里可能先被判有效、
  再被判失效，结论取决于模型侧的错误码。

---

## [v0.4.10] - 2026-09-20

<!-- summary: 修正「检测账号」的结论表述，说明凭据有效的判定依据 -->

### 修复

- **「检测账号」的结果更易于理解**：结论提示现在会说明判定依据
  （如「上游已接受该凭据」），不再单独暴露原始响应码。此前该接口会带上 400，
  与同一响应里的「凭据有效」自相矛盾，容易被误读成检测失败。

---

## [v0.4.9] - 2026-09-20

<!-- summary: 新增账号级停用与凭据检测；修复手机等窄屏下侧边栏与设置项被挤压 -->

### 新增

- **按账号停用**：账号卡片上新增停用开关，一键让某个账号不再参与自动轮询，再点一次恢复。

  | | 效果 |
  | :--- | :--- |
  | 停用后 | 自动轮询与「首选账号」不再选中它 |
  | 显式指定该账号的请求 | 仍然可用（停用针对轮询，不是禁止调用） |
  | 账号卡片上的显示 | 手动停用与巡检停用分别标注来源 |

- **检测账号**：账号卡片上新增按钮，就地发一次轻量鉴权请求，回报凭据是否有效。
  只报告结论、不改动配置，不消耗额度。

  可区分三种结果：凭据有效 / 凭据已失效（建议重新登录）/ 暂时没拿到结论（网络或上游异常）。

- **巡检增加凭据预检**：探测模型之前先确认账号凭据。凭据失效的账号，其名下模型
  不再逐个探测，也不再被写进模型禁用策略 —— 坏的是凭据不是模型。

  凭据确认失效时可自动停用该账号（可在设置里关闭）。探测不到结论（网络抖动、上游 5xx）时不会停用。

### 修复

- 修复**窄屏（手机）下设置页被挤成一条竖线**：侧边栏固定占 220px，在手机上吃掉近一半宽度，
  正文被压到只剩两百多像素。现在窄屏下侧边栏自动收起，宽屏恢复。

- 修复**窄屏下设置项的说明文字与右侧按钮互相挤压**：横排在窄屏下把文字压成竖排，
  现在改为上下堆叠，各自占满整行；「关于」面板的键值对同样改为上下排列。

---

## [v0.4.8] - 2026-09-20

<!-- summary: 巡检落盘改为只叠加本轮判定，巡检期间点下的手动禁用不再被覆盖 -->

### 修复

- 修复**巡检进行中做的手动禁用会消失**：一轮巡检要真实探测几百个组合、耗时以分钟计，
  此前是「开场读一次策略、结尾整份写回」。这几分钟里在账号卡片上点掉的模型，
  会被开场那份快照覆盖回去 —— 界面显示着已禁用，配置里却没有，刷新或重启后更会「自己恢复」。

  | | 修复前 | 修复后 |
  | :--- | :--- | :--- |
  | 巡检期间你在卡片上的改动 | 被开场快照覆盖 | 原样保留 |
  | 本轮的探测判定（不可用自动禁用 / 恢复自动启用） | 生效 | 生效，且只走自动通道 |

---

## [v0.4.7] - 2026-09-20

<!-- summary: 账号卡片把「手动禁用」与「巡检禁用」分开计数，巡检也不再探测手动禁用的模型 -->

### 修复

- 修复**账号卡片的「已禁用」把巡检禁用也算进去**：巡检自动禁用一个模型，「已禁用」也跟着 +1，
  与旁边的「巡检」指向同一批模型，看不出哪些是自己关的。现在两个计数互不重叠。

  | 徽标 | 修复前 | 修复后 |
  | :--- | :--- | :--- |
  | 已禁用 N | 手动禁用 + 巡检禁用 | 只数手动禁用的 |
  | 巡检 M | 其中有多少是巡检写的 | 只数巡检写的 |

- 修复**巡检结果重启后丢提示**：「凭据失效、本轮已跳过的账号」与被上游拒绝的探测错误码
  只在当轮存在、重跑补不回来，此前没有被存进那份供重启回显的摘要，于是重启后不再出现。

### 变更

- 巡检**不再探测手动禁用的模型**：这类组合整个不进入探测清单，省掉真实的上游请求与额度消耗。
  少掉的这部分会在巡检结果里单独说明（「手动禁用 N 项未探测」），而不是混进探测计数里。
  巡检自己写入的禁用项照常参与探测，模型恢复可用时仍会自动放开。

---

## [v0.4.6] - 2026-09-20

<!-- summary: 账号卡片里注入的两块固定收尾，「积分明细」不再一会儿在上一会儿在下 -->

### 修复

- 修复**账号卡片的排版会漂**：同一页里「近期到期 / 积分明细」有时在卡片中间、有时掉到卡片最底，
  「账号池控制条」与「可用模型」的先后也会互换。现在两块固定收尾，官方内容一律排在它们之前。

  | 卡片内的顺序 | 修复前 | 修复后 |
  | :--- | :--- | :--- |
  | 积分摘要 → 积分明细 → 控制条 → 可用模型 | 常有卡片不是这个顺序 | 每张卡片都一致 |

### 新增

- 构建流水线增加**账号卡片顺序回归测试**：模拟官方晚一步把明细追加进卡片，断言两块仍被拉回末尾，
  且只有官方内容的卡片不被搬动。

---

## [v0.4.5] - 2026-09-19

<!-- summary: 缓存命中率改为采用上游实测用量，版本号合并为一套，设置页「关于」重新排版 -->

### 新增

- 构建流水线增加**版本号一致性检查**：网关报出的版本必须与更新日志、发布说明对得上，否则不出镜像。
- 构建流水线增加**界面渲染冒烟测试**：注入前端的每个面板都必须渲染成功，脚本抛异常即构建失败。
- 构建流水线增加 **Token 用量统计测试**：缓存字段解析、跨数据块流式采集、命中率聚合与边界。

### 修复

- 修复**「缓存命中率」永远显示 0**：网关此前未读取上游返回的用量，输入与输出按字符数估算、
  缓存命中数恒为 0。现改为采用上游实测值。

  | 统计项 | 之前 | 现在 |
  | :--- | :--- | :--- |
  | 输入 / 输出 token | 按字符数估算 | 上游实测值 |
  | 缓存命中数 | 恒为 0 | 上游实测值 |
  | 缓存命中率 | 恒为 0% | 命中数 ÷ 输入 |

- 修复**版本号有两套**：面板显示 `v1.8.0`、发布页写 `v0.4.4`。现在网关版本与 GitHub 发布标签、
  镜像 tag 完全同号，构建期校验两者一致。

### 变更

- **设置页「关于」重新排版**：去掉「容器镜像」一块；「数据目录与端口」改为对齐的两列，
  分别列出数据目录 / 控制台 / 网关 API，数据目录可一键复制。
- 输入 / 输出改用上游实测值后，同一段对话的 token 计数与之前不同（通常更高）。

### 说明

- 历史记录里没有缓存明细，按「全部未命中」计入，命中率会随新调用逐步上升。
- 拿不到用量时（例如调用方中途断开）仍回退字符估算，并把命中记为 0。

---

## [v0.4.4] - 2026-09-19

<!-- summary: 巡检探测彻底对齐真实调用，并补上「探测被拒」「凭据失效」两道跳过规则 -->

### 修复

- 修复**巡检仍然会误禁好模型**：探测请求里还留着一个「输出上限」参数，上游对它设有最小值校验，
  导致同批账号下所有模型被判成不可用。现已彻底不带这类可选参数，探测请求与普通调用完全一致。

  | 探测请求的形态 | v0.4.2 | v0.4.3 | v0.4.4 |
  | :--- | :--- | :--- | :--- |
  | 流式请求 | ❌ | ✅ | ✅ |
  | 首条为系统提示词 | ❌ | ✅ | ✅ |
  | 不带可被校验的可选参数 | ❌ | ❌ | ✅ |
  | 一轮下来的结果 | 全部判为不可用 | 仍有一部分被误判 | 正常区分 |

### 新增

- **探测被上游拒绝时不再写配置**：请求参数被上游校验拒绝时逐条跳过；
  若整轮都是这种情况则整轮作废并提示错误码。
- **账号凭据失效不再牵连模型**：某个账号鉴权整体失效时本轮整体跳过，并在面板上提示重新登录。
- **面板显示「探测被拒」计数与「凭据失效账号」名单**。

### 变更

- 探测改为读到流式响应的**第一帧即断开**，不再依赖「输出上限」参数控制开销。
- 「上次巡检」的提示区分**整轮已跳过**与**部分组合被跳过**。

### 安全

- 只有「上游明确报出该模型不可用」才会写入禁用；参数被拒跳过、凭据失效跳过、整轮无可用信号作废。

---

## [v0.4.3] - 2026-09-19

<!-- summary: 修复巡检误报导致的成批误禁，禁用项区分「手动 / 巡检」来源，设置页补上「关于」 -->

### 修复

- 修复**巡检把好模型成批误禁**：探测请求未使用流式、首条消息也不是系统提示词，
  被上游以参数错误回绝后读成「模型不可用」。现已补齐两处形态，可用与真不可用能够区分。
- 修复**点「立即巡检」后界面不刷新**：结束后调用的刷新函数不存在，报错被静默吞掉。
- 修复**上次巡检结果随重启丢失**：结果改为落盘，重启后仍能看到上一轮统计。

### 新增

- **整轮误判保护**：一整轮里若没有任何组合可用且不可用数量达到门槛，判定为探测机制本身出了问题，
  整轮作废、不写任何配置，并在面板上提示「已跳过」及原因。
- **禁用项区分来源**：每个禁用项标明是「手动」还是「巡检」。

  | 来源 | 谁写的 | 探测到可用时 |
  | :--- | :--- | :--- |
  | 手动 | 你在账号卡片上点掉的 | 不动，不会被自动放开 |
  | 巡检 | 自动探测判定不可用后写入的 | 自动放开（自愈） |

- **账号卡片区分两种禁用态**：手动禁用为灰色删除线，巡检禁用为琥珀色删除线并提示会自动恢复。
- **巡检面板新增「当前禁用构成」**：手动禁用多少项、巡检禁用多少项。
- **设置页新增「关于」**：项目名称与版本、项目主页 / 更新日志 / 发布版本 / 问题反馈入口、
  容器镜像地址、数据目录与端口、当前规模、非官方项目声明。
- **巡检改动前自动留底**：落盘前先备份策略文件，出问题可整份回退。

### 变更

- 构建流水线在打包镜像**之前**跑一遍本地单测，测不过就不出镜像。
- 巡检结果日志统一口径：手动触发与定时轮次都打印组合数、可用 / 不可用 / 跳过与是否作废。

### 安全

- 旧版本的禁用清单在读取时一律视为「手动禁用」，升级不会把旧配置当成可自愈的项放开。
- 自动启用只作用于巡检自己写入的禁用项，任何情况下都不会放开手动禁用。

---

## [v0.4.2] - 2026-09-19

<!-- summary: 模型可用性自动巡检：调不通自动禁用，恢复自动启用 -->

### 新增

- **设置页「模型可用性巡检」面板**：`自动巡检` 总开关、`巡检间隔`（最短 5 分钟）、
  `巡检账号范围`（不选即全部）、`检测到可用时自动启用`，以及 `只探测用过的模型` 范围开关。
- **立即巡检**：一键跑一轮，不改变开关状态；面板回显本轮组合数、可用 / 不可用 / 跳过计数，
  以及新增禁用与启用项，并保留上一次的变更明细。
- **巡检接口**：`GET /model-health/config`、`PUT /model-health/config`、
  `GET /model-health/status`、`POST /model-health/run`。

### 变更

- **判定分三档**：不可用（`400/401/403/404/422/500/502/503/504`）自动禁用；可用（`200` 等）
  在「自动启用」开启时移出黑名单；`429` / `408` / `425` / 网络异常与超时一律跳过、不改变任何配置。
- **探测只发 1 个 token 并只看 HTTP 状态**，探测流量不会混进用量统计。
- **默认只探测该账号用过的模型**，新账号无调用记录时退回内置基础清单。
- **同一时刻只允许一轮巡检**，手动触发与定时轮次撞车时后来者被拒。
- **巡检不阻塞正常调用**：探测在独立线程中执行。

### 安全

- 巡检写入的就是手动禁用所用的同一份策略（`/data/.autobuddy/model_policy.json`），
  **关掉自动巡检后已写入的禁用项继续生效**。
- 巡检配置独立持久化在 `/data/.autobuddy/model_health_config.json`，缺失或损坏时回退为「默认关闭」。
- 巡检默认**关闭**，升级后保持升级前的手动禁用状态。

### 不改变的行为

账号管理、自动签到、账号池调度、Token 统计、既有模型别名路由，以及手动模型禁用，全部保持不变。

---

## [v0.4.1] - 2026-09-19

<!-- summary: 模型标签可点击禁用，精确到「账号 × 模型」 -->

### 新增

- **模型标签可点击**（账号卡片）：每个模型标签点一下变为「已禁用」（灰底 + 删除线），
  再点一下恢复。卡片标题旁显示 `已禁用 N` 徽章。支持 `Tab` + `Enter` / `Space` 键盘操作。
- **禁用真正生效于网关**：网关在挑账号之前先看这次请求用的是哪个模型，把禁用了该模型的账号直接排除。
- **模型策略接口**：`GET /account-models/config` 读取策略，`PUT /account-models/config` 切换禁用状态。

### 变更

- **标签三态**：默认（可用）/ 蓝底加粗（该账号真实调用过）/ 灰底删除线（已禁用），
  禁用态优先覆盖蓝色高亮。
- **只影响当前模型**：被禁用账号的其他模型完全不受影响。

### 修复

- **别名一并挡住**：禁用 `hy4` 会连同其目标模型 `hy3` 一起挡掉。
- **安全回退**：若某模型在所有账号上都被禁用，网关不返回「无可用账号」，
  而是回退到不过滤、让上游返回真实错误。
- **禁用状态不再静默丢失**：某账号「还没有调用记录、但已禁用过模型」时也纳入统计。

### 安全

- 禁用策略落盘 `/data/.autobuddy/model_policy.json`，与账号池配置完全分离、互不覆盖。
- **只记「被禁用」的项**，未列出的一律视为可用。
- 采用「先写临时文件再原子替换」，文件缺失、损坏或格式不对时一律回退为「全部可用」。

---

## [v0.4.0] - 2026-09-19

<!-- summary: 设置页新增 API 接入面板与访问密钥 -->

### 新增

- **设置 → API 接入 面板**：对外访问地址（按当前主机名自动推导，走域名或隧道时可手动改）、
  接口端点一键复制、网关状态概览、密钥校验开关、密钥管理、连通性自检、可直接使用的 `curl` 示例。
- **API 访问密钥**：格式 `sk-ab-` + 32 位十六进制。支持 `Authorization: Bearer …` 与 `x-api-key: …`
  两种请求头，`Bearer` 大小写不敏感，密钥首尾空格会被容忍。可新建、复制明文、停用、启用、删除、
  清空，每行显示掩码、调用次数与最近使用时间。
- **连通性自检**：在容器内回环实测「网关健康 / 模型清单 / 密钥配置自洽性」三步。

### 变更

- **密钥校验默认关闭**。没有配置密钥的用户行为与升级前完全一致，`/v1/*` 直接可调。

### 修复

- 修复**内部调用被宿主代理劫持**：`httpx` 默认读取宿主机的 `HTTP_PROXY` / `HTTPS_PROXY` /
  `ALL_PROXY`，被代理接管时容器内回环调用会返回 `404`，表现为「UI 打不开 / 模型清单为空」。
  现在 WebUI 代理的全部内部调用统一忽略代理环境变量；网关访问上游的客户端保持默认。

### 安全

- **缺失 / 错误 / 已停用的密钥一律 `401`**，并给出可读中文原因。
- **两道门锁防线**：一个可用密钥都没有时不允许开启校验；已开启校验时不允许停用或删除
  **最后一个**启用中的密钥，也不允许清空全部。
- **清空全部密钥时不会顺手把校验关掉。**
- **容器内回环免校验**：WebUI 代理与网关同容器，放行回环不会把外部请求放进来。
- 密钥文件落盘 `/data/.autobuddy/api_keys.json`（权限 `0600`），采用原子替换写入；
  文件损坏时回退到「未启用校验 + 空列表」。
- 密钥比对使用常量时间算法，避免时序侧信道。

---

## [v0.3.18] - 2026-09-19

<!-- summary: 修复账号调用次数随容器重建归零 -->

### 修复

- 修复**账号卡片「已调用 N 次」在容器重建后归零**：选账号流水此前只存在进程内存里，
  现在落盘到 `/data/.autobuddy/selection_logs.json`，跨重建连续。
- 修复**「清零统计」清不干净**：现在同时清内存与磁盘。

### 变更

- **语义澄清**：落盘只是让计数跨重启连续，**不是**变成历史累计值。
  它依然是**滑动窗口** —— 统计「最近 200 次选账号里各账号出现几次」，会随新请求滑动而衰减。

### 安全

- 写盘采用原子替换，进程被强制终止也不会留下半截损坏的文件。
- 文件缺失、损坏或结构不对时一律返回空列表，绝不抛异常；统计落盘失败不影响正常的 API 请求。

---

## [v0.3.17] - 2026-09-19

<!-- summary: 清理数据卷里 CLI 时代遗留的痕迹 -->

### 移除

一次性清理 CodeBuddy CLI 在数据卷里的遗留（删除前已打包备份到 `/mnt/user/appdata/autobuddy/backup/`）：

| 内容 | 体积 | 性质 |
| :--- | ---: | :--- |
| `.codebuddy/` 家目录 | 18 MB | CLI 设置、插件市场缓存、`projects/` `sessions/` `traces/` 等 |
| `.codebuddy-rotate/` | 8 KB | CLI 的凭证 helper 与状态文件 |
| CLI 钩子脚本与事件日志 | 约 1 KB | Stop / FinalStop 钩子及其事件记录 |
| `.npm/` 缓存 | 2.8 MB | 当初全局安装 CLI 留下的 npm 缓存 |

合计约 **21 MB**。

### 修复

- 修掉唯一还在「制造」残留的源头：`/data/.autobuddy/rotate` 是一个每次容器启动都会被重建的空目录，
  现在启动脚本不再创建它。

### 明确不动

- `/data/.config/CodeBuddy`、`/data/.config/CodeBuddy CN` —— 属 IDE 集成，官方二进制至今仍在写入。
- 官方的账号自动轮换配置与日志 —— 是官方二进制自带的功能（当前未启用）。

---

## [v0.3.16] - 2026-09-19

<!-- summary: 消除卡片上两个「调用次数」的口径混淆 -->

### 变更

- 卡片上原本同时存在两个「调用次数」，口径完全不同却极易混淆：模型标签里说的是
  「**模型**被调用几次」，账号池条上说的是「该**账号**被分摊到几次请求」。
- 模型标签的工具提示**去掉**调用次数，只保留用量信息（估算 Token / 积分）；
  模型区标题不再带「已调用 N」后缀，恒为 `可用模型 · N`。
- **保留**账号池条的 `已调用 N 次` —— 卡片上唯一的调用次数。
- **保留**模型标签的蓝色高亮。

---

## [v0.3.15] - 2026-09-19

<!-- summary: 更正 v0.3.14 的误判，并加改文案失效告警 -->

### 修复

- 更正 v0.3.14 的结论：该版把账号页副标题记作「用户可见的真问题」，实际响应层自 v0.3.8 起
  就在改写这句文案，用户看到的一直是改写后的版本。真实性质是**源码级残留**，修法是把它挪到源头。
- 删掉一条已经永远命不中的响应层替换规则。
- 修掉告警自身的误报：初版对 HTML 请求也做文案替换检查，而这几条只存在于 JS 资源里。

### 新增

- **文案替换零命中告警**：替换规则一条都没命中时打一行日志，便于通过 `docker logs` 发现
  上游改文案导致的静默失效。不抛异常，不会让请求变成 500。

---

## [v0.3.14] - 2026-09-19

<!-- summary: 修掉描述已移除产品的陈旧界面文案 -->

### 修复

- **账号管理页副标题**把 CodeBuddy IDE 与 CodeBuddy CLI 说成本页的管理对象，
  而这两个桌面端产品早已被物理移除。现在改为只描述容器里真实存在的两件事：
  「统一管理 WorkBuddy 账号、积分与签到状态，并为 API 网关提供账号。」

### 变更

- 改用更严的普查方法：把界面上**所有**中文文案抽出来全量过一遍，
  而不是只按「桌面 / CLI 关键词」筛。

### 明确不动

- 三处「疑似残留」经查实为不可达的死代码，不做处理。
- 卡片的「模型限额」徽章在容器内不可达（它扫描的是宿主机 IDE 日志，容器里没有）。

> ⚠️ 本节初版把该问题写成「用户可见的真问题」，是错的。详见 v0.3.15 的更正。

---

## [v0.3.13] - 2026-09-19

<!-- summary: 修掉空状态里指向已删除按钮的误导文案 -->

### 修复

- 账号列表为空时，界面引导用户去点**已经被删掉的**「导入本机账号」/「导入本机国际版账号」按钮，
  且「请确认本机已安装…」本身就是桌面端话术。现在改为指向容器里真实存在的入口
  （`OAuth 扫码添加` / `导入备份`）。

### 明确不动

- 另外 6 处文案经逐个追查「打开入口是否已被抹除」，确认都是不可达的死代码，不做处理。

---

## [v0.3.12] - 2026-09-18

<!-- summary: 清掉最后两处 CLI 残留 -->

### 移除

- **账号页的「CodeBuddy CLI 接入」引导横幅**：它的显示与否取决于官方后端返回的状态，
  只要后端改口，横幅就会带着「接入 CLI / 更新 CLI 认证 / 升级 CLI helper」按钮重新冒出来。
  现在从二进制里物理删除。
- **Token 统计里两个已失效的数据来源分类**：它们对应已移除的桌面 CLI / IDE，
  容器里不存在对应会话日志。

---

## [v0.3.11] - 2026-09-18

<!-- summary: 彻底移除 CLI；修复补丁静默失效 -->

### 修复

- 修复**补丁整体失效且失败被静默吞掉**：官方版本升级后重排了前端代码里的变量名，
  而补丁脚本把旧变量名写死在锚点里，导致每一条都失配；失配时脚本只打印一行提示就继续，
  构建脚本又把失败吞掉 —— 补丁一个字节都没改，镜像构建却照样成功。
- 现在**锚点不再依赖变量名**，一律改用稳定的**文案 / 类名 / 元素 id** 定位；
  并且**锚点失配 = 构建失败**，不会静默上线。
- 同时**锁定上游版本**，避免上游发版静默改变结构。

### 移除

- **彻底移除 CodeBuddy CLI（不是隐藏）**：镜像不再安装 CLI，删除对应的绑定引导逻辑与启动步骤，
  移除控制台里 CLI 相关的接口、样式与状态标记，Unraid 模板也随之删掉不再需要的挂载项。
- **设置页移除 5 个桌面专属区块**：权限检测、启动设置、自动更新、限额监听、CLI 自动轮换。
  **保留**「外观」与「自动签到」。

### 变更

- 项目定位明确为：**只做账号自动签到保活 + 提供 OpenAI 兼容 API**，除 `/data` 外不需要其他挂载。

---

## [v0.3.10] - 2026-09-18

<!-- summary: 补全容器内运行 CLI 的必要条件 -->

### 新增

- 为容器内置默认的 git 身份（system 级，可用 `git config --global` 覆盖），
  否则 CLI 的版本控制能力会直接失败。
- Unraid 模板新增可选的 `/workspace` 挂载位。

> 本版能力已在 v0.3.11 随 CLI 一并移除，仅作历史留档。

---

## [v0.3.9] - 2026-09-18

<!-- summary: 让 CodeBuddy CLI 在容器里真正可用 -->

### 新增

- 镜像内置 CodeBuddy CLI，并关闭容器内自动更新。
- 容器启动时自动接好认证链路，无需交互式登录；**不覆盖**已存在的凭证。
- 设置页实时显示探测到的 CLI 版本，区分「真的可用」与「只是配置文件存在」。

### 修复

- 官方界面的「已接入」判定只看配置文件是否存在，从不检测 CLI 二进制 ——
  容器里没装 CLI 也会显示「已接入」。现在改为真实探测。

> 本版能力已在 v0.3.11 随 CLI 一并移除，仅作历史留档。

---

## [v0.3.8] - 2026-09-18

<!-- summary: 全局巡检：修掉面板空数据与失真文案 -->

### 修复

- 修复**其他账号看不到模型调用提示**：上游接口只对当前账号返回模型明细，导致多个账号里
  只有一两个有调用记录。现在网关每次调用都记录实际服务该请求的账号，以网关自己归因为主数据源。
- 修复 **Token 统计页两个面板恒为空**：现在「用量分布」按账号聚合、「消耗最高的调用」按单次调用排列、
  趋势图可按模型筛选，全部有真实数据。
- 修复**云端流水合并后全部落进「未归属账号」并占据 100%**：现在补上账号归属；
  未归因的历史记录改标为「未归因（升级前记录）」。

### 变更

- **账号卡片移除冗余提示**：删掉「自动分配中，并发请求会分摊到 N 个已启用账号」文案与
  「清零统计」按钮，保留 `已调用 N 次` 计数徽章与「参与调用 / 设为首选」控件。
- **讲清「当前 CLI 账号」为什么只有一个**：它来自官方底层接口，官方只维护一个绑定账号。
  现在设置页注入范围说明，账号卡片也给被绑定的那张打上标记。
- **主页文案按容器真实能力重写**。

### 安全

- **响应禁用缓存**：HTML / JS 响应禁用缓存，避免浏览器用旧副本导致「改了却没生效」。

---

## [v0.3.7] - 2026-09-18

<!-- summary: 新增并发分摊观测，并修掉一处并发隐患 -->

### 新增

- **选账号流水**：每次选账号都记录一条（保留最近 200 条），并打一行日志，可直接 `docker logs` 核对。
- **观测接口**：查询各账号命中次数与最近明细，以及清零流水以便做干净的压测观测。

### 修复

- 修复**并发隐患**：选账号时「读序号 + 递增」不是原子操作，两个并发请求可能读到同一个序号而
  双双落到同一账号 —— 表现为「并发时其实只用了其中一个」。现在把这一步串起来。

### 变更

- 账号卡片新增 `已调用 N 次` 计数徽章与清零按钮。

---

## [v0.3.6] - 2026-09-18

<!-- summary: 补上卡片头部漏掉的桌面产品槽位 -->

### 修复

- v0.3.4 只移除了账号卡片**底部**的产品槽位，漏掉了卡片**头部账号名右侧的图标版**。
  现在整行物理移除。
- 修正上一版的验证方式：此前用的是固定字面量断言，而这些标签实际由表达式动态拼接，
  字面量在原始文件里出现次数本来就是 0，断言等于没验。现在改为用真实浏览器加载页面后检查 DOM。

---

## [v0.3.5] - 2026-09-18

<!-- summary: 修复手动指定账号时状态不更新 -->

### 修复

- 修复**「最近使用账号」记录缺失**：选账号有三条路径（自动轮询 / 手动固定 / 请求指定），
  但只有自动轮询那条会记录结果，于是走另外两条时状态里显示的仍是上一次自动分配的结果，
  看起来像「手动设置没生效」。现在三条路径统一记录，并新增「来源」字段。

### 新增

- 账号卡片为「最近一次真实调用所用账号」增加 `最近调用` 虚线标签。

---

## [v0.3.4] - 2026-09-18

<!-- summary: 卡片槽位物理切除 + 账号池并行调度 -->

### 移除

- 账号卡片底部的三个产品槽位（WorkBuddy / CodeBuddy IDE / CodeBuddy CLI）从二进制中
  **整块物理删除**，不再依赖任何界面遮掩。

### 新增

- **账号池：请求级并行调用**。一条对话请求仍由单个账号完成，但多个并发请求会分摊到不同账号。
- **参与调用开关**：每个账号可单独停用/启用；不设置时语义为「全部启用」，设置后即为白名单。
- **手动首选**：可固定只用某一个账号，再点一次回到自动分配。
- **单次请求指定账号**：可用请求头或请求体临时指定，不影响全局配置。
- **账号池接口**：查询状态与更新配置。

---

## [v0.3.3] - 2026-09-18

<!-- summary: 界面残留再清理，明细倒序与精简 -->

### 变更

- 彻底隐藏检测宿主机桌面客户端的「当前账号」状态徽章 —— 容器里它们恒为「未运行 / 未安装」。
- 请求明细改为按时间**倒序**展示，不再依赖上游流水的拼接顺序。
- 请求明细**精简为最近 200 条**，避免返回体过大；前端每页 50 条保持不变。

---

## [v0.3.2] - 2026-09-18

<!-- summary: 卡片模型清单改为动态可用清单 -->

### 变更

- **口径统一**：卡片展示的模型改为直接读取网关的完整可路由清单，与全局清单完全一致。
- **已调用高亮**：该账号真实调用过的模型单独着色，悬停显示调用次数与消耗积分。
- **新账号不再留空**：刚添加、还没有调用记录的账号同样展示完整清单。
- 完全不硬编码，上游新增模型会自动出现。
- **移除无效的桌面程序状态图标**：它们检测的是宿主机桌面客户端，容器内恒为「未运行」。

### 修复

- 修正镜像构建中残留的标签规则 —— 此前每次构建都会把旧版本标签覆盖到最新镜像上。
- 删除仓库根目录下未被镜像使用的重复代码文件，消除「改了不生效」的陷阱。

---

## [v0.3.1] - 2026-09-18

<!-- summary: 请求明细从弹窗改为页面内平铺 -->

### 变更

- 「请求明细」不再是弹出层，而是直接平铺在 Token 统计页面底部，
  呈现最近调用的模型、时间、会话标识、所属网关与用量构成比，保留分页与悬停详情。
- 清除右上角多余的「查看请求明细」弹窗按钮。

---

## [v0.3.0] - 2026-09-18

<!-- summary: 界面源码级精简 + Token 统计引擎上线 -->

### 新增

- **Token 统计**：网关层实时统计所有请求的输入/输出 Token、响应耗时与模型归类，
  并自动融合官方云端历史流水，提供每日趋势图、模型用量分布与明细列表。

### 移除

- **「导入本机账号」按钮**，不再产生无用 DOM 与控制台报错。
- **Token 统计上方的 4 个分类按钮**（对应桌面客户端）。
- **冗余的「Token 总览」提示标题**。
- **设置页的「权限检测」模块**。

### 变更

- 版本号正式递进为 `v0.3.0`，并开始维护本更新日志。

---

## [v0.2.0] - 2026-09-18

<!-- summary: 容器化体验升级与全量模型网关支持 -->

### 新增

- **侧边栏可折叠**：支持宽窄两种模式切换，并记忆状态。
- **全量前沿模型接入**：实测打通 25+ 款主流大模型与官方工作模式，
  涵盖混元 Hy3、DeepSeek-V3、OpenAI GPT-5.6 系列、Gemini-3.5、GLM-5.3、Kimi-K3、MiniMax-M3 等。
- 支持流式与非流式双向智能转换，可对接 Sub2API、OpenClaw、NextChat 等下游客户端。

### 修复

- 修正页面路由返回的文件类型不正确导致的**异常白屏与误下载**。
- 消除「在 Finder 中显示」「打开完全磁盘访问」等桌面端弹窗提示。

---

## [v0.1.2] - 2026-09-18

<!-- summary: 图标全链路对齐与 Sub2API 对接 -->

### 变更

- **图标全链路对齐**：侧边栏、网页顶栏与浏览器标签页统一使用高清圆角图标。
- **Sub2API 深度对接**：规范 OpenAI 格式渠道，支持全量模型映射。

---

## [v0.1.0] - 2026-09-18

<!-- summary: 初始版本发布 -->

### 新增

- **初始版本发布**：实现容器化基础运行环境与 OpenAI API 基础转换网关。

---

<!-- 链接区 -->

[未发布]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.8...HEAD
[v0.5.8]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.7...v0.5.8
[v0.5.7]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.6...v0.5.7
[v0.5.6]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.5...v0.5.6
[v0.5.5]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.4...v0.5.5
[v0.5.4]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.3...v0.5.4
[v0.5.3]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.2...v0.5.3
[v0.5.2]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.1...v0.5.2
[v0.5.1]: https://github.com/deltrivx/AutoBuddy/compare/v0.5.0...v0.5.1
[v0.5.0]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.17...v0.5.0
[v0.4.17]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.16...v0.4.17
[v0.4.16]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.15...v0.4.16
[v0.4.15]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.14...v0.4.15
[v0.4.14]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.13...v0.4.14
[v0.4.13]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.12...v0.4.13
[v0.4.12]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.11...v0.4.12
[v0.4.11]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.10...v0.4.11
[v0.4.10]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.9...v0.4.10
[v0.4.9]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.8...v0.4.9
[v0.4.8]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.7...v0.4.8
[v0.4.7]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.6...v0.4.7
[v0.4.6]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.5...v0.4.6
[v0.4.5]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.4...v0.4.5
[v0.4.4]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.3...v0.4.4
[v0.4.3]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.2...v0.4.3
[v0.4.2]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.1...v0.4.2
[v0.4.1]: https://github.com/deltrivx/AutoBuddy/compare/v0.4.0...v0.4.1
[v0.4.0]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.18...v0.4.0
[v0.3.18]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.17...v0.3.18
[v0.3.17]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.16...v0.3.17
[v0.3.16]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.15...v0.3.16
[v0.3.15]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.14...v0.3.15
[v0.3.14]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.13...v0.3.14
[v0.3.13]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.12...v0.3.13
[v0.3.12]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.11...v0.3.12
[v0.3.11]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.10...v0.3.11
[v0.3.10]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.9...v0.3.10
[v0.3.9]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.8...v0.3.9
[v0.3.8]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.7...v0.3.8
[v0.3.7]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.6...v0.3.7
[v0.3.6]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.5...v0.3.6
[v0.3.5]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.4...v0.3.5
[v0.3.4]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.3...v0.3.4
[v0.3.3]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.2...v0.3.3
[v0.3.2]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.1...v0.3.2
[v0.3.1]: https://github.com/deltrivx/AutoBuddy/compare/v0.3.0...v0.3.1
[v0.3.0]: https://github.com/deltrivx/AutoBuddy/compare/v0.2.0...v0.3.0
[v0.2.0]: https://github.com/deltrivx/AutoBuddy/compare/v0.1.2...v0.2.0
[v0.1.2]: https://github.com/deltrivx/AutoBuddy/compare/v0.1.0...v0.1.2
[v0.1.0]: https://github.com/deltrivx/AutoBuddy/releases/tag/v0.1.0
