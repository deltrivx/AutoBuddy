# 更新日志

本项目的所有重要变更都记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

**这份日志写「你能感知到什么」**，不写代码怎么改的。想了解某个版本的设计取舍与完整细节，
请点该版本标题旁的「详细说明」；想了解怎么部署、怎么接入，请看 [README](./README.md)。

---

## [未发布]

### 计划中

- 暂无。

---

## [v0.4.5] - 2026-09-19

<!-- summary: 缓存命中率不再恒为 0（改为采用上游真实用量），版本号合并为一套，设置页「关于」重新排版 -->

**统计面板上的数字变成真的，版本号也只剩一个。**

这一版新增的三道构建期检查（版本号一致性、Token 统计、界面渲染冒烟）与三处修复同等重要：
它们守的都是「出错了但看不出来」的那类问题。

### 修复

- **「缓存命中率」永远是 0**：网关此前既没有取上游随响应返回的用量，也没有采集
  缓存命中数 —— 输入与输出一直是按字符数估出来的（约 3.5 字符算 1 个 token），
  命中数恒为 0，于是不管实际命中多少，界面都只显示 0%。

  | 统计项 | 之前 | 现在 |
  | :--- | :--- | :--- |
  | 输入 / 输出 token | 按字符数估算 | 上游实测值 |
  | 缓存命中数 | 恒为 0 | 上游实测值 |
  | 缓存命中率 | 恒为 0% | 命中数 ÷ 输入 |

  两点需要说明：**历史记录**里没有缓存明细，按「全部未命中」计入，
  因此命中率会随新的调用逐步上升，不是一次到位；拿不到用量时（例如调用方中途断开）
  仍回退到字符估算，并把命中记为 0 —— 不知道就是不知道，不猜一个数出来。

### 变更

- **版本号合并为一套（重要）**：此前面板显示 `v1.8.0`、发布页写 `v0.4.4`，
  是两条并行的版本线，看见的人没法判断自己跑的是不是最新。
  现在网关报出的版本与 GitHub 发布标签、镜像 tag 完全同号：
  「关于」面板上的徽章，就是你在 Releases 里看到的那个号。
  构建流水线会校验两者一致，对不上直接失败，不会再出现「面板和发布页各说各话」。
- **设置页「关于」重新排版**：
  - 去掉「容器镜像」一块 —— 升级方式属于文档，镜像名与版本号本来就是同一件事，不必占两行；
  - 「数据目录与端口」改为对齐的两列，分别列出**数据目录 / 控制台 / 网关 API**，
    数据目录可一键复制。此前路径与端口挤在一行，换行后必然错位。
- 输入 / 输出改用上游实测值后，同一段对话统计出来的 token 数与之前不同（通常更高，
  因为中文按字符数折算一向偏低）。这是口径变准了，不是用量涨了。

### 新增

- **构建流水线增加「版本号一致性」检查**：网关报出的版本必须与更新日志里最新一段、
  发布说明文件对得上，否则不出镜像。版本号错位这类问题，靠人记是记不住的。
- **构建流水线增加界面渲染冒烟测试**：把注入到前端的脚本放进一套最小页面环境里跑一遍，
  每个面板都必须渲染成功。注入的脚本一旦抛异常，那块面板会**静默消失** ——
  没有报错、没有日志，页面看起来只是「少了一块」，只靠肉眼看很容易漏过去。
- **构建流水线增加 Token 用量统计的测试**：缓存字段解析、跨数据块的流式采集、
  命中率聚合与边界，都锁了断言。

---

## [v0.4.4] - 2026-09-19

<!-- summary: 巡检探测彻底对齐真实调用，并补上「探测被拒」「凭据失效」两道跳过规则 -->

**让巡检只在真正确认模型不可用时才写入禁用。**

### 修复

- **巡检仍然会误禁好模型（重要）**：v0.4.3 补齐了流式与首条消息两处形态，
  但探测请求里还留着一个「输出上限」参数 —— 上游对它设有最小值校验，
  于是同一批账号下的所有模型又一次被整体判成不可用，一轮下来新增 44 项误禁。
  现已彻底不带这类可选参数，探测请求与普通调用完全一致。

  | 探测请求的形态 | v0.4.2 | v0.4.3 | v0.4.4 |
  | :--- | :--- | :--- | :--- |
  | 流式请求 | ❌ | ✅ | ✅ |
  | 首条为系统提示词 | ❌ | ✅ | ✅ |
  | 不带可被校验的可选参数 | ❌ | ❌ | ✅ |
  | 一轮下来的结果 | 全部判为不可用 | 仍有一部分被误判 | 正常区分 |

### 新增

- **探测被上游拒绝时不再写配置**：请求参数被上游校验拒绝，说明问题出在探测本身，
  对模型可用与否**一无所知** —— 这类结果单独归类、逐条跳过，不会写进禁用清单。
  若整轮都是这种情况，整轮作废并提示错误码，而不是留下一屏误禁。
- **账号凭据失效不再牵连模型**：某个账号的上游鉴权整体失效（token 过期最常见）时，
  该账号本轮会被整体跳过，并在面板上点名提示重新登录；
  此前会把该账号名下所有模型逐个禁掉，而真正该做的只是重新登录。
- **面板显示「探测被拒」计数与「凭据失效账号」名单**，这两类情况不再伪装成平静的「什么都没变」。

### 变更

- 探测改为读到流式响应的**第一帧即断开**，不再依赖「输出上限」参数控制开销；
  一轮巡检的上游消耗比之前更低。
- 「上次巡检」的提示区分「整轮已跳过」与「部分组合被跳过」，异常不再显示成正常。

### 安全

- 现在只有「上游明确报出该模型不可用」才会写入禁用。参数被拒跳过、凭据失效跳过、
  整轮无可用信号作废 —— 三道闸门都指向同一个原则：宁可漏禁一轮，也不误禁一批。

---

## [v0.4.3] - 2026-09-19

<!-- summary: 修复巡检误报导致的成批误禁，禁用项区分「手动 / 巡检」来源，设置页补上「关于」 -->

**让巡检真的只禁坏掉的模型，并且不再碰你自己关掉的那些。**

### 修复

- **巡检把好模型成批误禁（重要）**：探测请求的形态与真实调用不一致 ——
  上游只接受流式请求、且要求首条消息是系统提示词，而探测两条都没满足，
  于是每一次探测都被上游以参数错误回绝，被读成「模型不可用」。
  一轮下来全部组合判为不可用，禁用清单从 46 项膨胀到 231 项。
  现已补齐流式与首条消息两处形态，可用的正常返回、真不可用的返回「模型服务不存在」，
  两者能区分开（参数层面还差的一处由 v0.4.4 收尾）。
- **点「立即巡检」后界面不刷新**：结束后调用的刷新函数不存在，报错被静默吞掉，
  跑完一轮后面板与账号卡片仍显示旧状态。
- **上次巡检结果随重启丢失**：结果改为落盘，容器重启后设置页仍能看到上一轮统计。

### 新增

- **整轮误判保护**：一整轮里若没有任何组合可用、且不可用数量达到门槛，
  判定为探测机制本身出了问题（凭据失效 / 上游整体故障），**整轮作废、不写任何配置**，
  并在面板上明确提示「已跳过」及原因。模型不会约好了同时失效 ——
  宁可漏禁一轮，也不误禁一批。
- **禁用项区分来源**：每个禁用项标明是「手动」还是「巡检」。

  | 来源 | 谁写的 | 探测到可用时 |
  | :--- | :--- | :--- |
  | 手动 | 你在账号卡片上点掉的 | 不动，不会被自动放开 |
  | 巡检 | 自动探测判定不可用后写入的 | 自动放开（自愈） |

  手动禁用通常不是「坏了」，而是「能用但不想用」（最常见的是控成本）。
  此前自动启用无法区分两者，只要模型可达就会把它移出黑名单 ——
  现在只放开自己写进去的那些，人的取舍不再被自愈抹掉。
- **账号卡片区分两种禁用态**：手动禁用为灰色删除线，巡检禁用为琥珀色删除线并提示会自动恢复；
  标题徽章拆出单独一枚「巡检 N」。
- **巡检面板新增「当前禁用构成」**：手动禁用多少项、巡检禁用多少项。
- **设置页新增「关于」**：项目名称与版本、项目主页 / 更新日志 / 发布版本 / 问题反馈入口、
  容器镜像地址（可一键复制）、数据目录与端口、当前规模（含禁用构成），以及非官方项目声明。
- **巡检改动前自动留底**：一轮巡检可能同时改动上百个条目，落盘前先备份策略文件，
  出问题时可整份回退，不必逐个点回来。

### 变更

- 构建流水线在打包镜像**之前**跑一遍本地单测，测不过就不出镜像。
- 巡检结果日志统一口径：手动触发与定时轮次都打印组合数、可用 / 不可用 / 跳过与是否作废。

### 安全

- **升级兼容**：旧版本的禁用清单在读取时一律视为「手动禁用」。此前没有巡检，
  清单全部来自人工点击，该归属是准确的 —— 升级不会把旧配置当成可自愈的项放开。
- 自动启用现在**只作用于巡检自己写入的禁用项**，任何情况下都不会放开手动禁用。

---

## [v0.4.2] - 2026-09-19

<!-- summary: 模型可用性自动巡检：调不通自动禁用，恢复自动启用 -->

**让网关自己定期去试：调不通的自动禁用，恢复的自动启用，仍然可以一键关掉。**

### 新增

- **设置页「模型可用性巡检」面板**：`自动巡检` 总开关、`巡检间隔`（最短 5 分钟）、
  `巡检账号范围`（不选即全部）、`检测到可用时自动启用`，以及 `只探测用过的模型` 范围开关。
- **立即巡检**：一键跑一轮，不改变开关状态；面板回显本轮组合数、可用 / 不可用 / 跳过计数，
  以及新增禁用与启用项，并保留上一次的变更明细。
- **巡检接口**：`GET /model-health/config`、`PUT /model-health/config`、
  `GET /model-health/status`、`POST /model-health/run`。

### 变更

- **判定分三档**：不可用（`400/401/403/404/422/500/502/503/504`）自动禁用；
  可用（`200` 等）在「自动启用」开启时移出黑名单；
  `429` / `408` / `425` / 网络异常与超时一律**跳过、不改变任何配置** ——
  上游高峰期限流是常态，把它当成故障会在高峰后留下一大批被误禁的好模型。
- **探测只发 1 个 token 并只看 HTTP 状态**：目的是确认链路通不通，不解析回答内容，
  探测流量也不会混进用量统计。
- **默认只探测该账号用过的模型**，新账号无调用记录时退回内置基础清单，避免组合数量失控。
- **同一时刻只允许一轮巡检**：手动触发与后台定时轮次撞车时后来者被拒，防止双倍消耗上游额度、
  并避免两轮各自读到的配置互相覆盖。
- **巡检不阻塞正常调用**：探测过程在独立线程中执行，网关转发不受影响。

### 修复

- 无。

### 安全

- 巡检写入的就是手动禁用所用的同一份策略（`/data/.wb-switch/model_policy.json`），
  **关掉自动巡检后已写入的禁用项继续生效**，仍可在账号卡片上手动启用 / 重新禁用。
- 巡检配置独立持久化在 `/data/.wb-switch/model_health_config.json`，
  与账号数据、账号池配置互不覆盖；缺失或损坏时回退为「默认关闭」。
- 巡检默认**关闭**，升级后保持升级前的手动禁用状态，不会擅自改动既有配置。

### 不改变的行为

账号管理、自动签到、账号池调度（并行轮询 / 手动首选 / 请求级指定账号）、Token 统计、
既有模型别名路由，以及 v0.4.1 的模型标签手动禁用，全部保持不变。

---

## [v0.4.1] - 2026-09-19

<!-- summary: 模型标签可点击禁用，精确到「账号 × 模型」 -->

**给模型加一个开关：可以只拉黑「某个账号的某个模型」，而不用停用整个账号。**

### 新增

- **模型标签可点击**（账号卡片）：每个模型标签点一下变为「已禁用」（灰底 + 删除线），
  再点一下恢复。卡片标题旁显示 `已禁用 N` 徽章，折叠时也能看出这里动过手。支持 `Tab` + `Enter` / `Space` 键盘操作。
- **禁用真正生效于网关**：网关在挑账号之前先看这次请求用的是哪个模型，把禁用了该模型的账号直接排除，
  而不是等请求发出去、上游报错再回头。
- **模型策略接口**：`GET /account-models/config` 读取策略，`PUT /account-models/config` 切换禁用状态。

### 变更

- **标签三态**：默认（可用）/ 蓝底加粗（该账号真实调用过）/ 灰底删除线（已禁用）。
  禁用态**优先覆盖**蓝色高亮，否则刚点掉的那个看起来像没反应。
- **只影响当前模型**：被禁用账号的其他模型完全不受影响，照常参与调用。

### 修复

- **别名一并挡住**：禁用 `hy4` 会连同其目标模型 `hy3` 一起挡掉，防止调用方换个写法绕过。
- **安全回退**：若某模型在**所有**账号上都被禁用，网关**不会**返回「无可用账号」，
  而是回退到不过滤、让上游返回真实错误 —— 在网关层编一个错误反而会掩盖真正的原因。
- **禁用状态不再静默丢失**：某账号「还没有任何调用记录、但已经被禁用过模型」时也会被正确纳入统计。

### 安全

- 禁用策略落盘 `/data/.wb-switch/model_policy.json`，与账号池配置**完全分离、互不覆盖**。
- **只记「被禁用」的项**，未列出的一律视为可用 —— 官方上新模型时天然就是可用状态，不需要迁移配置。
- 采用「先写临时文件再原子替换」，不会留下半截损坏的配置；文件缺失、损坏或格式不对时一律回退为
  「全部可用」，**绝不影响正常调用**。

### 不改变的行为

账号管理、自动签到、账号池调度（并行轮询 / 手动首选 / 请求级指定账号）、Token 统计与既有模型别名路由
全部保持不变。

---

## [v0.4.0] - 2026-09-19

<!-- summary: 设置页新增 API 接入面板与访问密钥 -->

**在设置页把「怎么连、用什么密钥连」讲清楚，不用再翻文档。**

### 新增

- **设置 → API 接入 面板**：对外访问地址（按当前主机名自动推导，走域名或隧道时可手动改）、
  接口端点一键复制、网关状态概览、密钥校验开关、密钥管理、连通性自检、可直接使用的 `curl` 示例。
- **API 访问密钥**：格式 `sk-wb-` + 32 位十六进制。支持 `Authorization: Bearer …` 与 `x-api-key: …`
  两种请求头，`Bearer` 大小写不敏感，密钥首尾空格会被容忍。可新建、复制明文、停用、启用、删除、清空，
  每行显示掩码、调用次数与最近使用时间。
- **连通性自检**：在容器内回环实测「网关健康 / 模型清单 / 密钥配置自洽性」三步，
  专门用来定位「UI 能打开但 API 调不通」。

### 变更

- **密钥校验默认关闭**。没有配置密钥的用户，行为与升级前完全一致，`/v1/*` 直接可调。
  只有把「强制 API 密钥校验」打开，才会开始拦截。

### 修复

- **修复内部调用被宿主代理劫持**：`httpx` 默认会读取宿主机上的 `HTTP_PROXY` / `HTTPS_PROXY` /
  `ALL_PROXY`。NAS 上为了下载或科学上网设全局代理很常见，一旦被代理接管，容器内的回环调用会被
  转发到代理上并返回 `404`，表现为「UI 打不开 / 模型清单为空」，但网关本身是好的。
  现在 WebUI 代理的**全部内部调用**统一忽略代理环境变量；而**网关访问上游模型服务的客户端保持默认**，
  那里恰恰需要代理环境变量才能出网。

### 安全

- **默认不校验**：纯内网可以一直不开；一旦端口暴露到公网或经过隧道转发，**务必打开**。
- **缺失 / 错误 / 已停用的密钥一律 `401`**，并给出可读中文原因。
- **两道门锁防线**：一个可用密钥都没有时不允许开启校验；已开启校验时不允许停用或删除
  **最后一个**启用中的密钥，也不允许清空全部。这四条挡的都是同一类事故 ——
  一键点下去，自己和所有下游客户端一起失去访问权限。
- **清空全部密钥时不会顺手帮你把校验关掉** —— 静默降低安全性比多一次点击更糟。
- **容器内回环免校验**：WebUI 代理（18090）与网关（18091）同容器，它需要读 `/v1/models`
  来渲染账号卡片的模型清单；若一并拦截，一开校验就会把 UI 自己弄坏。
  容器外的请求经 docker NAT 进来，源地址是网桥地址而非 `127.0.0.1`，因此放行回环不会把外部请求放进来。
- 密钥文件落盘 `/data/.wb-switch/api_keys.json`（权限 `0600`），采用原子替换写入；
  文件损坏或结构不对时回退到「未启用校验 + 空列表」，不会抛异常把网关打死。
- 密钥比对使用常量时间算法，避免时序侧信道。

### 不改变的行为

账号管理、自动签到、账号池调度、Token 统计与既有模型别名路由全部保持不变。

---

## [v0.3.18] - 2026-09-19

<!-- summary: 修复账号调用次数随容器重建归零 -->

**修复账号卡片上的「已调用 N 次」在容器重建后归零。**

### 修复

- 账号调用次数的流水此前只存在**进程内存**里，容器一重建就清零。
  现在落盘到 `/data/.wb-switch/selection_logs.json`，跨重建连续。
- 「清零统计」现在**同时清内存与磁盘**，否则下次重启会把旧流水又读回来，看起来像「清零没生效」。

### 变更

- **语义澄清（重要）**：落盘只是让计数**跨重启连续**，**不是**变成历史累计值。
  它依然是**滑动窗口** —— 统计的是「最近 200 次选账号里各账号出现几次」，会随新请求滑动而衰减。

### 安全

- 写盘采用原子替换，进程被强制终止也不会留下半截损坏的文件。
- 文件缺失、损坏或结构不对时一律返回空列表，**绝不抛异常**；
  **统计落盘失败绝不影响正常的 API 请求**。

> 顺带说明：卡片上另一个计数（模型维度的 `gatewayCalls`）本来就存在磁盘上，
> 跨重建一直保留，只是自 v0.3.16 起前端不再显示它。

---

## [v0.3.17] - 2026-09-19

<!-- summary: 清理数据卷里 CLI 时代遗留的痕迹 -->

**清理数据卷里 CodeBuddy CLI 时代遗留的痕迹。**

### 移除

CodeBuddy CLI 早在 v0.3.11 就从镜像里移除了，但它在**数据卷**里留下的痕迹一直没清。
本版做一次性清理，删除前已打包备份到 `/mnt/user/appdata/workbuddy-switch/backup/`：

| 内容 | 体积 | 性质 |
| :--- | ---: | :--- |
| `.codebuddy/` 家目录 | 18 MB | CLI 设置、插件市场缓存、`projects/` `sessions/` `traces/` 等 |
| `.codebuddy-rotate/` | 8 KB | CLI 的凭证 helper 与状态文件 |
| CLI 钩子脚本与事件日志 | 约 1 KB | Stop / FinalStop 钩子及其事件记录 |
| `.npm/` 缓存 | 2.8 MB | 当初全局安装 CLI 留下的 npm 缓存 |

合计约 **21 MB**。

### 修复

- 修掉了唯一还在「制造」残留的源头：`/data/.wb-switch/rotate` 是一个**每次容器启动都会被重建**的空目录，
  光删文件治不了，必须改启动脚本。现在不再创建它。

### 明确不动

两项经核实**不属于** CLI 残留，保持原样：

- `/data/.config/CodeBuddy`、`/data/.config/CodeBuddy CN` —— 属 **IDE** 集成，官方二进制至今仍在写入；
- 官方的账号自动轮换配置与日志 —— 是官方二进制**自带**的功能（当前未启用），与 CLI 无关。

---

## [v0.3.16] - 2026-09-19

<!-- summary: 消除卡片上两个「调用次数」的口径混淆 -->

**消除账号卡片上两个「调用次数」同名不同义的混淆。**

### 变更

- 卡片上原本同时存在两个「调用次数」，口径完全不同却极易混淆：
  模型标签里说的是「**模型**被调用几次」，账号池条上说的是「该**账号**被分摊到几次请求」。
- 现在模型标签的工具提示**去掉**调用次数，只保留用量信息（估算 Token / 积分）；
  模型区标题不再带「已调用 N」后缀，恒为 `可用模型 · N`。
- **保留**账号池条的 `已调用 N 次` —— 卡片上唯一的调用次数，不再有二义。
- **保留**模型标签的蓝色高亮，颜色变化已足够表达「这个模型被调用过」。

---

## [v0.3.15] - 2026-09-19

<!-- summary: 更正 v0.3.14 的误判，并加改文案失效告警 -->

**更正 v0.3.14 的一处错误判断，并补上「改文案静默失效」的告警。**

### 修复

- **更正 v0.3.14 的结论**：该版把账号页副标题记作「用户可见的真问题」，这是错的。
  响应层自 v0.3.8 起就在改写这句文案，用户看到的一直是改写后的版本。
  真实性质是**源码级残留**，修法是把它挪到源头，而不是当作用户可见缺陷来对待。
- **删掉一条已经永远命不中的响应层替换规则**（源头改掉后它就没用了）。
- **修掉告警自身的误报**：初版对 HTML 请求也做文案替换检查，而这几条是前端界面文案、
  只存在于 JS 资源里，导致每个 HTML 请求都误报。

### 新增

- **文案替换零命中告警**：替换规则一条都没命中的时候会打一行日志，
  方便通过 `docker logs` 发现上游改文案导致的静默失效。不抛异常，不会让请求变成 500。

---

## [v0.3.14] - 2026-09-19

<!-- summary: 修掉描述已移除产品的陈旧界面文案 -->

**二轮界面文案普查，找出并修掉一处描述已移除产品的陈旧文案。**

> ⚠️ **本节初版把该问题写成「用户可见的真问题」，是错的。** 详见 v0.3.15 的更正：
> 用户看到的一直是响应层改写后的版本，真实性质是源码级残留。

### 修复

- **账号管理页副标题**里把 CodeBuddy IDE 与 CodeBuddy CLI 说成本页的管理对象，
  而这两个桌面端产品早在 v0.3.11 就被物理移除。现在改为只描述容器里真实存在的两件事：
  「统一管理 WorkBuddy 账号、积分与签到状态，并为 API 网关提供账号。」

### 变更

- 改用更严的普查方法：v0.3.13 只按「桌面 / CLI 关键词」筛，
  漏掉了**把已移除产品当作功能卖点写进正常句子**的文案（不含任何关键词）。
  本轮改为把界面上**所有**中文文案抽出来全量过一遍。

### 明确不动

- 三处「疑似残留」经查实为**不可达的死代码**（弹窗状态恒为假、或唯一入口已被移除），不做处理。
- 卡片的「模型限额」徽章在容器内**不可达**（它扫描的是宿主机 IDE 日志，容器里没有）。

---

## [v0.3.13] - 2026-09-19

<!-- summary: 修掉空状态里指向已删除按钮的误导文案 -->

**继续验收：修掉账号列表空状态里指向已删除按钮的误导文案。**

### 修复

- 账号列表为空时，界面会引导用户去点**已经被删掉的**「导入本机账号」/「导入本机国际版账号」按钮，
  而且「请确认本机已安装…」本身就是桌面端话术。
  现在改为指向容器里真实存在的入口（`OAuth 扫码添加` / `导入备份`）。

### 明确不动

- 另外 6 处文案经逐个追查「打开入口是否已被抹除」，确认都是**不可达的死代码**，不做处理。

---

## [v0.3.12] - 2026-09-18

<!-- summary: 清掉最后两处 CLI 残留 -->

**验收补漏：把最后两处 CLI 残留清掉。**

### 移除

- **账号页的「CodeBuddy CLI 接入」引导横幅**：它的显示与否取决于官方后端返回的状态。
  v0.3.11 只删掉了横幅里的按钮，横幅本身没进移除清单；当前后端恰好让条件为假所以它没显示，
  但只要后端改口，横幅就会带着「接入 CLI / 更新 CLI 认证 / 升级 CLI helper」按钮重新冒出来。
  现在从二进制里物理删除。
- **Token 统计里两个已失效的数据来源分类**：它们对应已移除的桌面 CLI / IDE，
  容器里不存在对应会话日志，是纯死重。保留的两条分类是真实存在的产品线。

---

## [v0.3.11] - 2026-09-18

<!-- summary: 彻底移除 CLI；修复补丁静默失效 -->

**彻底移除 CodeBuddy CLI，功能范围收敛为「账号自动签到保活 + 提供 OpenAI 兼容 API」。**

### 修复

本版起于一个线上现象：**每个账号前面又出现了三个桌面图标**。排查后发现问题比表象严重得多。

- **根因：补丁整体失效，且失败被静默吞掉。** 官方版本升级后重排了前端代码里的变量名，
  而补丁脚本把旧变量名写死在锚点里，于是**每一条都失配**；失配时脚本只打印一行提示就继续，
  构建脚本又把失败吞掉 —— **补丁一个字节都没改，镜像构建却照样成功**。
- 现在**锚点不再依赖变量名**，一律改用稳定的**文案 / 类名 / 元素 id** 定位；
  并且**锚点失配 = 构建失败**，不会像以前那样静默上线。
- 同时**锁定上游版本**，避免上游发版静默改变结构后又一次「构建成功但补丁全失效」。

### 移除

- **彻底移除 CodeBuddy CLI（不是隐藏）**：镜像不再安装 CLI，删除对应的绑定引导逻辑与启动步骤，
  移除控制台里 CLI 相关的接口、样式与状态标记，Unraid 模板也随之删掉不再需要的挂载项。
  镜像体积随之回落。
- **设置页移除 5 个桌面专属区块**：权限检测（检测 macOS 完全磁盘访问）、启动设置（开机静默启动到托盘）、
  自动更新（容器里靠镜像重建升级）、限额监听（依赖扫描桌面日志与安装桌面 hook）、
  CodeBuddy CLI 自动轮换（随 CLI 一并移除）。
  **保留**「外观」（浅色/深色主题，WebUI 里真实可用）与「自动签到」（核心功能）。

### 变更

- 项目定位明确为：**只做账号自动签到保活 + 提供 OpenAI 兼容 API**，除 `/data` 外不需要其他挂载。

---

## [v0.3.10] - 2026-09-18

<!-- summary: 补全容器内运行 CLI 的必要条件 -->

**补全容器内运行 CLI 的必要条件。**（本版能力已在 v0.3.11 随 CLI 一并移除，仅作历史留档。）

### 新增

- 为容器内置默认的 git 身份（system 级，可用 `git config --global` 覆盖），
  否则 CLI 的版本控制能力（查看 diff、提交变更、创建分支）会直接失败。
- Unraid 模板新增可选的 `/workspace` 挂载位，作为可操作的项目文件目录。

---

## [v0.3.9] - 2026-09-18

<!-- summary: 让 CodeBuddy CLI 在容器里真正可用 -->

**让 CodeBuddy CLI 在容器里真正可用。**（本版能力已在 v0.3.11 随 CLI 一并移除，仅作历史留档。）

### 新增

- 镜像内置 CodeBuddy CLI，并关闭容器内自动更新（自动更新会在重建时丢失且拖慢启动）。
- 容器启动时自动接好认证链路，无需交互式登录；**不覆盖**已存在的凭证，因此不影响手动选择。
- 设置页实时显示探测到的 CLI 版本，区分「真的可用」与「只是配置文件存在」。

### 修复

- 官方界面的「已接入」判定只看配置文件是否存在，从不检测 CLI 二进制 ——
  容器里没装 CLI 也会显示「已接入」。现在改为真实探测。

---

## [v0.3.8] - 2026-09-18

<!-- summary: 全局巡检：修掉面板空数据与失真文案 -->

**全局巡检：把「半成品面板」和「说不清的状态」一次清掉。**

### 修复

- **其他账号看不到模型调用提示**：上游接口**只对当前账号返回模型明细**，
  导致多个账号里只有一两个有调用记录。现在网关每次调用都把实际服务该请求的账号记录下来，
  以**网关自己归因**为主数据源，实测全部账号都能显示各自调用过的模型。
- **Token 统计页两个面板恒为空**：后端返回的三个字段一直是空数组，而前端正是读它们来渲染的。
  现在「用量分布」按账号聚合、「消耗最高的调用」按单次调用排列、趋势图可按模型筛选，全部有真实数据。
- **云端流水合并后全部落进「未归属账号」并占据 100%**：现在补上账号归属；
  未归因的历史记录改标为「未归因（升级前记录）」。

### 变更

- **账号卡片移除冗余提示**：删掉「自动分配中，并发请求会分摊到 N 个已启用账号」文案与「清零统计」按钮，
  保留 `已调用 N 次` 计数徽章与「参与调用 / 设为首选」控件。
  观测入口没丢，仍在接口里，只是不再往每张卡片上塞按钮。
- **讲清「当前 CLI 账号」为什么只有一个**：它来自官方底层接口，官方只维护**一个**绑定账号，
  本质是单账号概念。现在设置页注入范围说明（只影响 CLI 助手、不影响网关调用），
  账号卡片也给被绑定的那张打上标记，不再看起来「只有它有状态」。
- **主页文案按容器真实能力重写**：原来宣称管理容器里已不存在的桌面产品，措辞失真。

### 安全

- **响应禁用缓存**：前端资源文件名带 hash、不随改动变化，浏览器可能一直用旧副本，
  表现为「改了却没生效」。现在 HTML / JS 响应禁用缓存。

---

## [v0.3.7] - 2026-09-18

<!-- summary: 新增并发分摊观测，并修掉一处并发隐患 -->

**回答一个问题：多个账号在并发调用时是真并行分摊，还是只用了其中一个？**

### 新增

- **选账号流水**：每次选账号都记录一条（保留最近 200 条），并打一行日志，可直接 `docker logs` 核对。
- **观测接口**：查询各账号命中次数与最近明细，以及清零流水以便做干净的压测观测。

### 修复

- **修掉一个并发隐患**：选账号时「读序号 + 递增」不是原子操作，
  两个并发请求可能读到同一个序号而双双落到同一账号 —— 表现为「并发时其实只用了其中一个」。
  现在把这一步串起来。

### 变更

- 账号卡片新增 `已调用 N 次` 计数徽章与清零按钮。

> 实测结论（20 并发）：20 个请求**均匀分摊到全部 5 个账号，各 4 次**；
> 并发耗时 3.91s 对比单请求均值 3.33s，确为并发而非串行。

---

## [v0.3.6] - 2026-09-18

<!-- summary: 补上卡片头部漏掉的桌面产品槽位 -->

**补上真正的那两个按钮。**

### 修复

- v0.3.4 只移除了账号卡片**底部**的产品槽位，漏掉了卡片**头部账号名右侧的图标版** ——
  用户实际看到的「账号后面一个 CodeBuddy IDE 和 CodeBuddy CLI 按钮」，指的正是头部这一行。
  现在整行物理移除。
- **为什么上一版没验出来**：v0.3.4 的验证用的是固定字面量断言，
  而这些标签实际由表达式动态拼接，字面量在原始文件里出现次数本来就是 0，断言等于没验。
  现在改为用真实浏览器加载页面后检查 DOM。

---

## [v0.3.5] - 2026-09-18

<!-- summary: 修复手动指定账号时状态不更新 -->

**修复「最近使用账号」记录缺失。**

### 修复

- 选账号有三条路径（自动轮询 / 手动固定 / 请求指定），但只有**自动轮询**那条会记录结果。
  于是走「请求指定」或「手动固定」时，状态里显示的仍是上一次自动分配的结果，
  看起来像「手动设置没生效」。现在三条路径统一记录，并新增「来源」字段，
  可直接看出本次选择来自请求指定、手动固定还是自动轮询。

### 新增

- 账号卡片为「最近一次真实调用所用账号」增加 `最近调用` 虚线标签，便于直观确认账号池是否按预期分配。

---

## [v0.3.4] - 2026-09-18

<!-- summary: 卡片槽位物理切除 + 账号池并行调度 -->

**账号卡片上的桌面产品槽位物理切除，并上线账号池并行调度。**

### 移除

- 账号卡片底部的三个产品槽位（WorkBuddy / CodeBuddy IDE / CodeBuddy CLI）从二进制中**整块物理删除**，
  不再依赖任何界面遮掩 —— 此前两次遮掩方案在异步渲染时机下都被证实不可靠。

### 新增

- **账号池：请求级并行调用**。一条对话请求仍由单个账号完成（避免上下文与计费错乱），
  但**多个并发请求会分摊到不同账号**，不再全部挤在同一个账号上。
- **参与调用开关**：每个账号可单独停用/启用；不设置时语义为「全部启用」，设置后即为白名单。
- **手动首选**：可固定只用某一个账号，再点一次回到自动分配。
- **单次请求指定账号**：可用请求头或请求体临时指定，不影响全局配置。
- **账号池接口**：查询状态与更新配置。

---

## [v0.3.3] - 2026-09-18

<!-- summary: 界面残留再清理，明细倒序与精简 -->

**容器界面残留再清理，并优化请求明细的排序与体积。**

### 变更

- 彻底隐藏检测宿主机桌面客户端的「当前账号」状态徽章 —— 容器里它们恒为「未运行 / 未安装」，
  只产生误导。
- 请求明细改为按时间**倒序**展示，不再依赖上游流水的拼接顺序。
- 请求明细**精简为最近 200 条**，避免返回体过大；前端每页 50 条保持不变。

---

## [v0.3.2] - 2026-09-18

<!-- summary: 卡片模型清单改为动态可用清单 -->

**账号卡片模型清单改为动态可用清单。**

### 变更

- **口径统一**：卡片展示的模型改为直接读取网关的完整可路由清单，
  与全局清单完全一致 —— 此前会出现「全局显示 32 款、卡片只显示已调用的 23 款」的差异。
- **已调用高亮**：该账号真实调用过的模型单独着色，悬停显示调用次数与消耗积分。
- **新账号不再留空**：刚添加、还没有调用记录的账号同样展示完整清单。
- 完全不硬编码，上游新增模型会自动出现。
- **移除无效的桌面程序状态图标**：它们检测的是宿主机桌面客户端，容器内恒为「未运行」，
  悬停提示只会误导，已整组移除。

### 修复

- 修正镜像构建中残留的标签规则 —— 此前每次构建都会把旧版本标签覆盖到最新镜像上。
- 删除仓库根目录下未被镜像使用的重复代码文件，消除「改了不生效」的陷阱。

---

## [v0.3.1] - 2026-09-18

<!-- summary: 请求明细从弹窗改为页面内平铺 -->

**请求明细从弹窗重构为页面内平铺展示。**

### 变更

- 「请求明细」不再是弹出层，而是直接平铺在 Token 统计页面底部，
  呈现最近调用的模型、时间、会话标识、所属网关与用量构成比，保留分页与悬停详情。
- 清除右上角多余的「查看请求明细」弹窗按钮，整页浑然一体。

---

## [v0.3.0] - 2026-09-18

<!-- summary: 界面源码级精简 + Token 统计引擎上线 -->

**容器环境深度提纯，并上线 Token 统计全链路数据引擎。**

### 新增

- **Token 统计**：网关层实时统计所有请求的输入/输出 Token、响应耗时与模型归类，
  并自动融合官方云端历史流水，提供每日趋势图、模型用量分布与明细列表，
  告别空白报表。

### 移除

- **「导入本机账号」按钮**：不再产生无用 DOM 与控制台报错。
- **Token 统计上方的 4 个分类按钮**（对应桌面客户端），直接聚合呈现全量数据。
- **冗余的「Token 总览」提示标题**，页面更紧凑。
- **设置页的「权限检测」模块**（针对本地桌面路径的权限校验，容器里不存在该能力）。

### 变更

- 版本号正式递进为 `v0.3.0`，并开始维护本更新日志。

---

## [v0.2.0] - 2026-09-18

<!-- summary: 容器化体验升级与全量模型网关支持 -->

**体验升级，并打通往上游全量顶级模型的能力。**

### 新增

- **侧边栏可折叠**：支持宽窄两种模式切换，并记忆状态。
- **全量前沿模型接入**：实测打通 25+ 款主流大模型与官方工作模式，
  涵盖混元 Hy3、DeepSeek-V3、OpenAI GPT-5.6 系列、Gemini-3.5、GLM-5.3、Kimi-K3、MiniMax-M3 等。
- 支持流式与非流式双向智能转换。
- 容器启动自动完成 CLI 认证配置引导，消除「未接入 CLI」报警。（该能力已在 v0.3.11 移除）

### 修复

- 修正页面路由返回的文件类型不正确导致的**异常白屏与误下载**。
- 消除「在 Finder 中显示」「打开完全磁盘访问」等桌面端弹窗提示。

---

## [v0.1.2] - 2026-09-18

<!-- summary: 图标全链路对齐与 Sub2API 对接 -->

### 变更

- **图标全链路对齐**：侧边栏、网页顶栏与浏览器标签页统一使用高清圆角图标。
- **Sub2API 深度对接**：规范 OpenAI 格式渠道，支持全量模型映射。

---

## [v0.1.0] - 2026-09-18

<!-- summary: 初始版本发布 -->

### 新增

- **初始版本发布**：实现容器化基础运行环境与 OpenAI API 基础转换网关。

---

<!-- 链接区 -->

[未发布]: https://github.com/deltrivx/workbuddy-switch/compare/v0.4.3...HEAD
[v0.4.5]: https://github.com/deltrivx/workbuddy-switch/compare/v0.4.4...v0.4.5
[v0.4.4]: https://github.com/deltrivx/workbuddy-switch/compare/v0.4.3...v0.4.4
[v0.4.3]: https://github.com/deltrivx/workbuddy-switch/compare/v0.4.2...v0.4.3
[v0.4.2]: https://github.com/deltrivx/workbuddy-switch/compare/v0.4.1...v0.4.2
[v0.4.1]: https://github.com/deltrivx/workbuddy-switch/compare/v0.4.0...v0.4.1
[v0.4.0]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.18...v0.4.0
[v0.3.18]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.17...v0.3.18
[v0.3.17]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.16...v0.3.17
[v0.3.16]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.15...v0.3.16
[v0.3.15]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.14...v0.3.15
[v0.3.14]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.13...v0.3.14
[v0.3.13]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.12...v0.3.13
[v0.3.12]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.11...v0.3.12
[v0.3.11]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.10...v0.3.11
[v0.3.10]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.9...v0.3.10
[v0.3.9]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.8...v0.3.9
[v0.3.8]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.7...v0.3.8
[v0.3.7]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.6...v0.3.7
[v0.3.6]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.5...v0.3.6
[v0.3.5]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.4...v0.3.5
[v0.3.4]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.3...v0.3.4
[v0.3.3]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.2...v0.3.3
[v0.3.2]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.1...v0.3.2
[v0.3.1]: https://github.com/deltrivx/workbuddy-switch/compare/v0.3.0...v0.3.1
[v0.3.0]: https://github.com/deltrivx/workbuddy-switch/compare/v0.2.0...v0.3.0
[v0.2.0]: https://github.com/deltrivx/workbuddy-switch/compare/v0.1.2...v0.2.0
[v0.1.2]: https://github.com/deltrivx/workbuddy-switch/compare/v0.1.0...v0.1.2
[v0.1.0]: https://github.com/deltrivx/workbuddy-switch/releases/tag/v0.1.0
