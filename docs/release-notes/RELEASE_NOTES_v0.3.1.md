## 🌟 改进亮点

### 1. 「请求明细」从弹窗重构为页面内平铺展示

- **彻底消除弹出层**：将原桌面端弹窗遮罩彻底重构为 Token 统计页面底部的原生独立排版卡片。
- **自然流畅的页面排版**：打开「Token 统计」页面，下方直接平铺呈现最近调用的模型、时间、
  会话标识、所属网关与用量构成比。
- **支持完整分页**：内嵌表格保留 50 条/页的分页与鼠标悬停详细用量提示。
- **清除多余操作按钮**：同步清理右上角冗余的「查看请求明细」弹窗按钮，整页浑然一体。

### 2. 打包机制完善

- 持续遵循无损替换标准，确保底层运行时与前端界面稳定可靠。

---

## 不改变的行为

账号管理、自动签到与账号池调度保持不变。
